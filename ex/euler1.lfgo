; Project Euler 1
(import "fmt")
(defn problem1 () ||
  (def sum 0)
  (for (def i 1) (< i 1000) (++ i)
    (if (or (== (% i 3) 0) (== (% i 5) 0)) (+= sum i)))
  (fmt.Println sum))

(main
  (problem1))
